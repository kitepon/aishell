# 0.7.5

- 複数ファイル読取りで予算を対象間に配り、先頭の大きいファイルだけで後続が見えなくなる挙動を修正した。行範囲と期待SHAの指定にも対応した。
- 検索結果の周辺コードがMCP応答から欠けていた問題を修正し、同じ優先度のquery・ファイルを交互に返すようにした。
- snapshotの埋込本文で大きい案内を飛ばして古い小さいテストを選ぶ挙動を修正した。注目するファイルはcontext_pathsで指定できる。
- 予算、未返却量、continuation、SHA/cursor、runの完全log保持を維持する。

固定した実在repoで、3対象の先頭抜粋は5回から1回、2 queryの周辺コード取得は10回から1回になった。これは固定scriptの呼出し回数とJSON応答量の比較であり、provider token削減率ではない。条件と数値はdocs/evidence/context-usability-20260913.mdに記録した。

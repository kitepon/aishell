# 0.7.7

- OS再起動などでデバイス番号が変わると、同じ作業フォルダの編集を`RESERVED_NAMESPACE_CONFLICT`で拒否する不具合を修正した。保存済みのデバイス番号と現在値の照合を廃止し、既存markerの書換えや削除を不要にした。
- 中断した編集の復旧、容量予約、Trashへの退避にも同じ修正を適用した。操作中の同一ボリューム確認、inodeと内容SHAによる競合検出は維持する。
- 古いデバイス番号が残る状態で、通常編集、確定前の巻戻し、確定後の復旧、同一内容のrename、容量予約の再開、Trash移動前後の復旧を検証した。
